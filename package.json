{
  "private": true,
  "workspaces": [
    "src",
    "test",
    "docs-builder",
    "examples/simple",
    "examples/boids",
    "examples/particle-system"
  ],
  "scripts": {
    "build": "yarn workspace flock-ecs build",
    "build:dev": "yarn workspace flock-ecs dev",
    "build-examples": "yarn workspace simple build && yarn workspace boids build && yarn workspace particle-system build",
    "docs": "yarn workspace docs-builder build",
    "docs:dev": "yarn workspace docs-builder dev",
    "lint": "yarn workspace test lint",
    "test": "yarn workspace test test",
    "test:dev": "yarn workspace test test:dev"
  }
}
