name: CI

on:
  pull_request:
    branches:
    - master
    - dev

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: install yarn
        uses: borales/actions-yarn@v2.0.0
      - name: Test
        run: yarn test
  Lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: install yarn
        uses: borales/actions-yarn@v2.0.0
      - name: Lint
        run: yarn lint
